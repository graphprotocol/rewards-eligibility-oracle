apiVersion: monitoring.coreos.com/v1
kind: PodMonitor
metadata:
  name: rewards-eligibility-oracle
  labels:
    app: rewards-eligibility-oracle
spec:
  selector:
    matchLabels:
      app: rewards-eligibility-oracle
  podMetricsEndpoints:
  - port: metrics
    path: /metrics
    interval: 30s
    scrapeTimeout: 10s
